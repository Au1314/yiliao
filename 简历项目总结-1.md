# 简历项目总结：医疗疾病数据分析大屏可视化系统

> 本文档用于将该项目的经历写入简历，重点突出「用数据体现干了什么」。
> 数据口径均已从项目代码与数据库中核实：**835 条病例 / 23 类疾病 / 14 个字段 / 9 套分析模块 / 10 类图表 / 6 个后端接口（含 NL2SQL 问答）**。

---

## 一、项目概述

一个覆盖「数据采集 → 数据存储 → 数据分析 → 可视化展示 → 机器学习预测 → AI 对话问答」全流程的医疗疾病数据分析大屏系统。

| 环节 | 核心技术 | 具体工作 | 代码位置 |
|---|---|---|---|
| ① 数据采集（爬虫） | Python + requests + lxml（XPath）+ Selenium | 爬取好大夫网站 **835 条**真实病例（感冒/高血压/骨折等 23 类疾病），每条含疾病类型、性别、年龄、病情描述、医生、医院、科室等 **14 个字段**；详情页通过 selenium debuggerAddress 复用已登录浏览器，抓取身高、体重、患病时长、过敏史 | `spiders/` |
| ② 数据存储 | MySQL 8.0（medicalinfo 库 / cases 表） | 建表、CSV 中转、批量入库，自增 ID 至 835 | `medicalinfo.sql` |
| ③ 数据分析 | Python 后端实时聚合 | **9 个统计维度**：年龄段分布（7 档）、疾病类型 Top6、男女患病占比、科室分布、热门医院、各疾病平均身高体重、高频关键词、患病时长分布、过敏史分布 | `utils/getAllData.py` |
| ④ 可视化大屏 | Vue 2 + ECharts 5.4 + data-view + echarts-wordcloud + axios | **10+ 种图表**：南丁格尔玫瑰图（年龄占比）、疾病类型胶囊图、男女对比活动环图 + 水球图、科室环形图、关键词词云、身高体重均值图、患病时长柱状图、过敏史柱状图，外加 **6 个核心 KPI** 与病例滚动列表；**关键词云点击联动**：点击疾病名，年龄/性别/科室/时长/过敏史 5 图联动筛选 | `front-end-template-1-master/src/views/Index.vue` |
| ⑤ 病情预测 | jieba 分词 + TF-IDF + 随机森林 | 按病情文本自动分类疾病类型：停用词过滤 → TF-IDF（10000 特征）→ 随机森林（100 棵树），训练/测试 8:2 划分，测试集准确率 91.6% | `machine/tree.py` |
| ⑥ 后端 API | Flask + Flask-CORS + jsonify | **6 个接口**：/api/home、/getHomeData、/api/table、/tableData、/submitModel、/api/chat，统一 JSON 输出并解决跨域 | `app.py` |
| ⑦ AI 对话问答 | LangGraph + 智谱 GLM（glm-4.5-air） | 构建 NL2SQL 数据分析 Agent：自然语言提问 → 自动生成只读 SQL → 执行查询 → 中文回答，内置 SQL 注入拦截与只读校验，将静态大屏升级为对话式数据分析系统 | `agent/nl2sql.py` |

---

## 二、简历条目（可直接粘贴）

### 版本 A：数据分析方向（推荐主线）

> **医疗疾病数据分析大屏可视化系统**（个人项目 | Python · MySQL · ECharts · Vue · LangGraph）
>
> - 采集并清洗 **835 条**真实医疗病例数据（高血压、骨折等 **23 类疾病，14 个字段**），基于 XPath + 正则 + Selenium 实现列表页→详情页两级自动爬取与结构化入库；
> - 基于 MySQL 数据开发 **7 套统计分析模块**（年龄分布、性别占比、科室分布、热门医院、疾病 Top6、身高体重均值、关键词提取），由后端实时聚合计算后经 RESTful API 输出；
> - 搭建 **10 类可视化图表大屏**（南丁格尔玫瑰图、科室环形图、关键词词云、男女对比水球图、患病时长/过敏史柱状图、KPI 数字指标等），数据驱动渲染、随数据库自动刷新，支撑 **6 项核心指标**实时展示，并实现**关键词云点击联动**（点疾病名联动年龄/性别/科室/时长/过敏史 5 张图）；
> - 使用 **jieba + TF-IDF（10000 特征）+ 随机森林（100 棵树）** 训练病情分类模型，实现基于文本描述自动预测疾病类型（训练/测试集 8:2 划分，准确率 91.6%）；
> - 基于 **LangGraph + 智谱 GLM** 构建 **NL2SQL 数据分析 Agent**，用户自然语言提问即可自动生成 SQL、查询数据库并给出分析结论，内置只读安全校验，将静态大屏升级为**对话式数据分析系统**。

### 版本 B：后端 / Python 方向（偏工程）

> **医疗疾病数据分析大屏可视化系统**（个人项目 | Flask · PyMySQL · scikit-learn · LangGraph）
>
> - 编写 **requests + lxml + Selenium 三层爬虫**，完成列表页 → 详情页两级数据采集，数据经 CSV 中转批量写入 MySQL，总量 **835 条 / 14 字段**，详情页复用浏览器登录态获取补充信息；
> - 设计 Flask RESTful API（**6 个接口**），经 jsonify 统一输出 JSON、配置 CORS 解决跨域，前端 axios 异步对接，支持 GET/POST 双方法；
> - 构建 **jieba 分词 → TF-IDF（10000 特征）→ RandomForest（100 棵树）** 文本分类流水线，实现病情描述自动归属疾病类型之一；
> - 基于 **LangGraph ReAct Agent + 智谱 GLM** 实现 **NL2SQL**：封装只读 SQL 工具 + 危险关键字拦截，模型自主生成并执行查询、抽取结论，返回 answer / sql / data 三部分结果；
> - 全程使用 Git 版本管理，模块划分清晰（spiders / utils / machine / agent / 前端分离）。

### 版本 C：前端 / 可视化方向（偏展示）

> **医疗疾病数据分析大屏可视化系统**（个人项目 | Vue 2 · ECharts · data-view）
>
> - 基于 Vue 2 + ECharts + data-view 搭建 **1080P 数据大屏**，集成 **10 类图表**（玫瑰图 / 环形图 / 词云 / 水球图 / 柱状图 / 滚动表格 / 胶囊图等）与 **6 个实时 KPI**，动态展示 835 条病例的 9 个分析维度；
> - 通过 axios 对接后端 API 实现数据驱动渲染，图表随数据自动刷新，并对图表交互（tooltip、联动高亮、轮播切换、关键词云点击联动筛选）做了定制优化；
> - 辅助页面含病例详情滚动展示页、病情预测输入页与**智能问答对话页**（展示 AI 生成的 SQL 与结果表格），构成完整的大屏 + 功能页应用。

---

## 三、量化清单（用数据体现「干了啥」）

| 量化点 | 实际数字（已核实） | 简历写法建议 |
|---|---|---|
| 病例数据量 | 835 条（`SELECT COUNT(*) FROM cases`） | 「835 条真实病例」 |
| 数据字段 | 14 个（type/gender/age/content/身高/体重/患病时长/过敏史…） | 「14 字段结构化数据」 |
| 疾病类型 | 23 类：感冒、高血压、骨折、颈椎病、糖尿病、胃炎、抑郁症等 | 「覆盖 23 类疾病」 |
| 分析维度 | 9 个统计函数（getPieData / getConfigOne / getFoundData / getGenderData / getCircleData / getBodyData / getIllDurationData / getAllergyData 等） | 「9 套统计分析模块」 |
| 图表数量 | Index.vue 中 10 个展示模块 | 「10 类图表 / 6 项 KPI」 |
| API 接口 | 6 个 route（含 /api/chat） | 「6 个 RESTful 接口」 |
| ML 配置 | TF-IDF 10000 特征、随机森林 100 棵树、8:2 划分、准确率 91.6% | 「10000 维特征 / 100 棵树 / 91.6% 准确率」 |
| AI Agent | LangGraph + 智谱 GLM（glm-4.5-air）的 NL2SQL 对话式分析 | 「NL2SQL 数据分析 Agent」 |

---

## 四、注意事项（务必阅读）

1. **数据量口径已核实**：数据库中现有 **835 条病例 / 23 类疾病**，简历写「835 条 / 800+ 条」均成立，面试答 `SELECT COUNT(*) FROM cases;` 结果一致，可放心使用。

2. **「个人项目」标注要谨慎**：前端目录 `front-end-template-1-master` 表明大屏模板可能基于开源模板（data-view 为 @jiaminghi 开源组件库）。建议简历表述为「基于开源模板二次开发 + 自主完成数据采集、数据分析、机器学习、后端 API、AI Agent」，把爬虫 / 分析 / 模型 / API / NL2SQL 作为核心贡献。

3. **预测功能已上线**：`/submitModel`、`/api/submit` 已重新接入 `machine/tree.py` 的预测流水线，测试集准确率 **91.6%**，可现场演示。

4. **AI 问答接口**：`/api/chat` 基于 LangGraph + 智谱 GLM 实现 NL2SQL，需在 `.env` 配置 `ZHIPU_API_KEY` 才能调用；内置只读 SQL 校验，可讲「安全设计」卖点。

5. **面试必被追问的点（提前准备答案）**：
   - 爬虫遇到反爬 / 登录怎么处理？→ Selenium `debuggerAddress` 复用已登录浏览器会话；
   - TF-IDF 为什么选 10000 特征？随机森林为什么 100 棵树？→ 控制特征维度 / 兼顾精度与训练速度；
   - 为什么用 roseType 玫瑰图而非普通饼图？→ 以面积强调占比差异，突出高发年龄段；
   - NL2SQL 如何防止模型生成危险 SQL？→ 只允许 SELECT / WITH 开头，拦截 insert/update/delete/drop 等关键字，结果限量 200 行；
   - 数据清洗怎么做的？→ 正则提取身高体重数字、缺值统一为「无」，枚举字段归类。

6. **摆位建议**：
   - 目标「数据分析 / AI 应用」岗 → 放项目经验第一条，篇幅 3–4 行粗体 + 3–4 条 bullet，重点讲 NL2SQL Agent；
   - 目标「爬虫 / 后端」岗 → 突出爬虫架构 + API 设计 + ML 流水线 + Agent 工程；
   - 技能栏同步出现 `Python / MySQL / ECharts / Vue / scikit-learn / LangGraph` 与项目呼应，形成证据链；
   - 附 GitHub 链接或大屏截图（1 张）效果最佳。

---

## 五、面试自我介绍 1 分钟讲稿（项目部分）

> 我做了一个医疗疾病数据分析大屏系统。整体是一条从数据采集到可视化、再到机器学习、最后到 AI 对话问答的完整链路。
> 首先用 Python 写爬虫从好大夫网站抓了 835 条真实病例，覆盖高血压、骨折、颈椎病等 23 类疾病、14 个字段，存在 MySQL 里；
> 后端用 Flask 写了 6 个接口，实时聚合出年龄分布、性别占比、热门科室、疾病 Top6 等 7 套统计数据；
> 前端用 Vue 和 ECharts 搭了一个大屏，包含玫瑰图、环形图、词云、水球图等 8 类图表和 6 个核心指标；
> 我还用 jieba + TF-IDF + 随机森林训练了一个病情分类模型，输入病情描述就能预测疾病类型，准确率 91.6%；
> 最后基于 LangGraph 和智谱 GLM 做了一个 NL2SQL 数据分析 Agent，用户用自然语言提问，它能自动生成 SQL、查库并给出分析结论，把静态大屏升级成了对话式数据分析系统。
> 这个项目最大的收获是从采集、存储、分析到展示、预测、AI 问答的全链路搭建经验，以及把数据转化为决策信息的能力。

---

*生成日期：2026-09-10 · 基于项目代码与 medicalinfo.sql 实际数据核实（2026-09-20 更新为 835 条 / 23 类 + NL2SQL Agent）*
