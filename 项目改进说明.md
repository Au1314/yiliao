# 医疗疾病数据分析大屏系统 — 改进说明

> 本次改进目标：修复既有 bug + 把「静态大屏」升级为「对话式数据分析 Agent（NL2SQL）」。
> 完成日期：2026-09-20

---

## 一、改进总览

| 类型 | 内容 | 状态 |
|---|---|---|
| Bug 修复 | 数据库密码硬编码不一致、身高体重计算崩溃、年龄桶重复、模型路径硬编码、预测接口禁用 | ✅ 已修复 |
| 新功能 | NL2SQL 数据分析 Agent（LangGraph + 智谱 GLM） | ✅ 已实现 |
| 新功能 | `/api/chat` 接口 + 前端「智能问答」对话页 | ✅ 已实现 |
| 新功能 | 数据大屏新增患病时长/过敏史图表 + 关键词云点击联动筛选 | ✅ 已实现 |
| 工程化 | 配置统一到 `config.py` + `.env`，新增 `.gitignore` | ✅ 已完成 |

---

## 二、修复的 5 个 Bug

### 1. 数据库密码硬编码且不一致
- **问题**：`utils/query.py` 用 `023658`，`machine/tree.py` 用 `123456`（后者是错的），换环境必挂。
- **修复**：新建 `config.py` 统一读取数据库配置，`query.py` / `tree.py` 全部改为引用 `config.py`。密码存于 `.env`（已加入 `.gitignore`），`config.py` 不再内置明文密码。

### 2. `getBodyData` 身高体重计算崩溃 + 均值错误
- **位置**：`utils/getAllData.py` 原第 165 行 `if caseItem[10] and caseItem[11] == '无'`
- **问题**：`and` 逻辑错误，且 `int('无')` 会直接抛异常导致接口崩溃；均值也错误地除以「疾病总人数」而非「有效条数」。
- **修复**：新增 `_to_num()` 安全解析（`无`/空串/非数字统一返回 `None`），分别累加身高、体重并记录各自有效条数，均值按有效条数计算。

### 3. `getPieData` 年龄桶重复 key
- **位置**：`utils/getAllData.py` 原第 6 行，`'10-20岁'` 重复定义两次。
- **修复**：删除重复项。

### 4. `tree.py` 硬编码路径 + 重复 import
- **问题**：硬编码 Windows 路径 `d:\基于python...`、`import os` 和 `current_dir` 重复两遍、数据库密码错误。
- **修复**：路径改为 `os.path.join(os.path.dirname(__file__), ...)` 相对定位；清理重复代码；密码走 `config.py`。

### 5. 预测功能被禁用
- **位置**：`app.py` 原 `/api/submit`、`/submitModel` 返回 "temporarily disabled"。
- **修复**：重新接入 `machine/tree.py` 的 `predict()`；模型用 joblib 持久化（`model.pkl` / `vectorizer.pkl`），首次调用自动训练。**实测测试集准确率 91.6%**。

---

## 三、新功能：NL2SQL 数据分析 Agent

### 原理
用户用自然语言提问 → LangGraph 的 ReAct Agent 自动生成 SQL → 执行**只读**查询 → 用中文回答，同时返回生成的 SQL 和查询结果。

### 技术栈
- **框架**：LangGraph `create_react_agent`（版本 1.2.11）
- **大模型**：智谱 GLM `glm-4.5-air`（通过 `langchain-openai` 的 OpenAI 兼容接口调用）
- **工具**：`run_sql`（只读 SQL 执行，含安全拦截）

### 核心文件
- `agent/nl2sql.py` — Agent 主体（模型 + 工具 + prompt + 结果抽取）
- `config.py` — 数据库 + 大模型配置
- `.env` — 本地密钥（含智谱 API Key，已加入 `.gitignore`）

### 安全设计
- 只允许 `SELECT` / `WITH` 开头的语句
- 拦截 `insert/update/delete/drop/alter/create/truncate` 等危险关键字
- 查询结果最多返回 200 行，防止超大 payload

### 实测效果
| 问题 | 答案 | 生成的 SQL（节选） |
|---|---|---|
| 一共有多少条病例？哪种疾病最多？ | 835 条，感冒最多（70 人，8.4%） | `SELECT type, COUNT(*) ... GROUP BY type ORDER BY count DESC LIMIT 1` |
| 高血压患者的男女比例？ | 男 23 / 女 22 ≈ 1.05:1 | `SELECT gender, COUNT(*) ... WHERE type LIKE '%高血压%' GROUP BY gender` |
| 哪个科室接诊最多？ | 骨科 79 人 | `SELECT department, COUNT(*) ... GROUP BY department ... LIMIT 1` |
| 40 岁以上糖尿病多少人？ | 10 人 | `... WHERE type LIKE '%糖尿病%' AND CAST(age AS UNSIGNED) >= 40` |

---

## 四、文件变更清单

### 新增
- `config.py` — 集中配置（数据库 + 大模型）
- `.env` — 本地密钥（勿提交）
- `.env.example` — 配置模板
- `.gitignore` — 忽略 `.env` / 模型文件 / node_modules 等
- `agent/nl2sql.py` — NL2SQL Agent
- `front-end-template-1-master/src/views/Chat.vue` — 智能问答对话页

### 修改
- `app.py` — 重新启用预测接口、新增 `/api/chat`、端口改为 8080
- `utils/query.py` — 改用 `config.py`，连接用完即关
- `utils/getAllData.py` — 修复 `getBodyData`、`getPieData`
- `machine/tree.py` — 重写：路径修复 + joblib 持久化 + 密码走 config
- `front-end-template-1-master/src/router/index.js` — 新增 `/chat` 路由

---

## 五、运行说明

### 依赖安装（已装好，无需重复）
```bash
.venv\Scripts\python.exe -m pip install flask flask-cors pymysql sqlalchemy \
  jieba scikit-learn pandas numpy joblib wordcloud matplotlib \
  langgraph langchain-openai langchain-core
```

### 配置 API Key
在项目根目录 `.env` 文件的 `ZHIPU_API_KEY=` 后填入智谱 API Key（已填好）。
获取地址：https://open.bigmodel.cn → 控制台 → API Keys

### 启动后端
```bash
.venv\Scripts\python.exe app.py        # 监听 5000 端口
```

### 启动前端
```bash
cd front-end-template-1-master
npm run serve
```

### 接口一览
| 接口 | 说明 |
|---|---|
| `/getHomeData` | 大屏首页数据（含修复后的身高体重均值） |
| `/submitModel` / `/api/submit` | 病情分类预测 |
| `/api/chat` | **NL2SQL 智能问答**（新增） |
| `/tableData` / `/api/table` | 病例表格 |

---

## 六、注意事项

1. **API Key 安全**：Key 存在 `.env`，已加入 `.gitignore`，切勿上传到公开仓库或发给他人。
2. **数据已更新**：数据库现为 **835 条 / 23 类疾病**（非简历中的 345 条 / 7 类），简历量化数字建议更新。
3. **前端未在浏览器实测**：后端接口已通过 Flask test client 全量验证，前端 `Chat.vue` 遵循现有 Vue2 写法，但尚未在浏览器点开确认，建议本地跑一遍。
4. **依赖 Python 版本**：本项目现在跑在 Python 3.13（虚拟环境），原 `requirements.txt` 里 pandas 1.4.3 / numpy 1.23.1 等旧版本在 3.13 下装不上，已改用最新版。若需复现旧环境请用 Python 3.8。

---

## 七、简历更新建议（可选）

在原有简历基础上，可新增/强化两点：

1. **数据量**：345 条 → **835 条 / 23 类疾病**。
2. **新增 AI Agent 卖点**：
   > 基于 LangGraph + 智谱 GLM 构建 **NL2SQL 数据分析 Agent**，用户自然语言提问即可自动生成 SQL、查询数据库并给出分析结论，内置只读安全校验，将静态大屏升级为**对话式数据分析系统**。

---

## 八、数据大屏可视化增强（第二轮）

在 AI Agent 升级之后，进一步增强了数据大屏的可视化能力。

### 新增两张图表
- **患病时长分布**：`utils/getAllData.py` 新增 `getIllDurationData()`，将患病时长归并为「一周内 / 一月内 / 半年内 / 大于半年 / 无」5 档；`illDuration` 字段 59% 为脏数据（纯数字），已剔除，仅基于 345 条有效记录统计。
- **过敏史分布**：新增 `getAllergyData()`，合并同类过敏原为「无过敏史 / 青霉素类 / 头孢类 / 青霉素+头孢类 / 其他」5 类。
- 前端 `Index.vue` 底部由 1 张图扩展为 3 张并排柱状图（身高体重 / 患病时长 / 过敏史）。

### 图表交互联动
- 点击关键词云图中的疾病名称 → 联动筛选年龄占比、男女对比、科室环形图、患病时长、过敏史 5 张图。
- 纯前端实现（复用已加载的 `casesData` 客户端过滤，无需后端二次请求），顶部状态栏支持「× 重置」。

### 修复
- Vue2 `<transition>` 包两个子元素会报「can only be used on a single element」并导致 `echarts.init` 拿不到 DOM（`invalid dom`），已改为 `v-if/v-else` + `loading` 布尔值。

---

*生成日期：2026-09-20*
